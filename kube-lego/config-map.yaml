apiVersion: v1
metadata:
  name: kube-lego
  namespace: kube-system
data:
  # Set this to a valid e-mail
  lego.email: "admin@company.com"
  lego.url: "https://acme-v01.api.letsencrypt.org/directory"
kind: ConfigMap